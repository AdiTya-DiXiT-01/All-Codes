#include <bits/stdc++.h>

using namespace std;

bool pair(int arr[], int n, int k)
{
    int low = 0;
    int high = n-1;

    while (low<high)
    {
        
    }
    
}

int main()
{
    int n;
    cin >> n;
    int a[n];

    for (int i = 0; i < n; i++)
        cin >> a[i];

    
    return 0;
}